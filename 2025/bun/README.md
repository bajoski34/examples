## Bun Runtime - Faster than Node.Js.

The Javascript runtime has a docker image on docker hub called `oven/bun`

```shell
bun init # start a bun application.
```

```shell
bun run start                 # run the `start` script
bun install <pkg>             # install a package
bun build ./index.tsx         # bundle a project for browsers
bun test                      # run tests
bunx cowsay 'Hello, world!'   # execute a package
```

## Supported Templates

You can find an exhstive list [here](https://bun.com/docs/runtime/templating/init#project-templates)

